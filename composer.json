{
    "name": "karakani/mecha",
    "description": "php wrapper for MeCab",
    "type": "library",
    "license": "MIT License",
    "autoload": {
        "psr-4": {
            "Karakani\\MeCab\\": "src"
        }
    },
    "require": {
        "php": ">=7.0"
    },
    "scripts": {
        "test-phpunit-phar": [
            "if [ ! -f ./phpunit.phar ]; then wget -O ./phpunit.phar https://phar.phpunit.de/phpunit-$(php -r 'echo PHP_VERSION_ID < 70300 ? 7 : 9;').phar ; chmod +x ./phpunit.phar ; fi",
            "./phpunit.phar"
        ]
    }
}
